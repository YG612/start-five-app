============================================================
使用说明
============================================================

将以下文件放入仓库：

1. docs/CODEX_P15R_P18_UNIFIED_INTERACTION_AND_PAGE_EXPERIENCE_SPEC.md
2. references/current_quick_add.jpg
3. references/current_quick_edit.jpg

一次只给 Codex 一个阶段。P15R SOURCE_GATE 未通过前不得开始 P15。
没有 Android 设备时，P15R RELEASE_GATE 写 PENDING_EXTERNAL，但 SOURCE_GATE 通过后可以继续后续源码阶段。

============================================================
首次准备指令
============================================================

请读取：

- docs/NEXT_CHAT_HANDOFF.md
- docs/CODEX_QUADRANT_REFACTOR_SPEC.md
- docs/CODEX_P7_P10_USER_METRIC_SPEC.md
- docs/CODEX_P7_P10_PROGRESS.md
- docs/CODEX_P11_P14_USER_VALUE_OPTIMIZATION_SPEC.md
- docs/CODEX_P11_P14_PROGRESS.md
- docs/CODEX_P15R_P18_UNIFIED_INTERACTION_AND_PAGE_EXPERIENCE_SPEC.md
- references/current_quick_add.jpg
- references/current_quick_edit.jpg

当前基线：

- P0-P14 已完成。
- 40 suites / 247 tests PASS。
- TypeScript strict、Android lintInternal、assembleInternal 已通过。
- 当前 APK SHA-256：cd720dd410bfede498d6f9fdaf9f67fcb2b714b87e69577911575797e627270d。
- Android DEVICE / UTEST 为 PENDING_EXTERNAL。
- iOS 为 PENDING_MACOS_XCODE。
- P14-02B 备份合并仍为 BLOCKED_SPEC_GUARD。
- 当前目录可能不是 Git 工作树；检测一次后不要反复运行失败的 Git 命令。
- package.json 当前没有 JavaScript ESLint script，不得安装 ESLint 只为凑命令。

本文件取代旧版 CODEX_P15_P18_PAGE_FOCUS_GROWTH_EXPERIENCE_SPEC.md；发生冲突时以新文件为准。

创建 docs/CODEX_P15R_P18_PROGRESS.md，只记录真实路径、阶段状态、Schema/backup version、测试、构建和未完成风险。不要复述整份规格，不等待确认，不重做 P0-P14。

============================================================
P15R 指令：发布阻断级核心交互修复
============================================================

只执行规格中的：

P15R-00 至 P15R-09。

必须解决：

1. 统一真实可拖动的 Bottom Sheet；删除巨大“关闭任务面板”按钮。
2. Sheet 打开时隐藏或完全禁用底部“象限/专注/成长/我的”。
3. 快速添加增加固定“添加任务”；保存成功自动退出。
4. 空标题退出不创建；非空标题退出安全保存一次；未选象限的全局草稿进入待判断。
5. 快速编辑默认突出“先做 5 分钟”；完成、重新安排、需要帮助分级；复制/长期计划/移动/删除进入更多。
6. 删除用户可见的“重要度 80、手动紧急度 25、截止时间联动”等内部语言。
7. 修复第一小步空状态；进度只显示 0/25/50/75，100% 由完成任务负责。
8. 普通用户长按节点后直接跟手拖动，松手保存；不得再弹“移动到哪个象限”。
9. 地图任务标题使用密度自适应紧凑标签，不再普遍只显示前 3-4 个字。
10. 清理“先记下”和“+”等重复入口、长期教程、顶部密度、Safe Area、卡片墙和同权按钮。
11. 对所有任务相关 Sheet 做同类问题审计，不只修两张截图。

禁止开始 P15。

完成后运行：

pnpm exec tsc --noEmit
pnpm test -- --runInBand
cd android
gradlew.bat :app:lintInternal :app:assembleInternal

要求：

- 原 247 项继续通过。
- 新增 tests/p15r-core-interaction/。
- 输出 P15R SOURCE_GATE 结果。
- 有设备则执行 DEVICE/UTEST；无设备写 RELEASE_GATE=PENDING_EXTERNAL。
- 按规格第 17 节格式汇报，并更新进度文件。

============================================================
P15 指令：页面分工与视觉减法
============================================================

前提：P15R SOURCE_GATE 已通过。

只执行 P15-00 至 P15-06。

目标：

1. 象限只回答“现在做什么”。
2. 移除象限首屏完整成长物和“种子 0/3”。
3. “今天轻一点”变为上下文帮助，不作为永久主按钮。
4. 象限首屏任何状态只有一个最高权重主行动。
5. 专注页建立“当前/下一专注 + 快速开始 + 今天安排”页面壳。
6. 成长页建立“成长物 + 今日 3 项 + 本周最多 3 项 + 一条建议”页面壳。
7. 我的页面按六组整理。
8. 统一视觉 Token、间距、按钮、深色、无障碍和页面状态。

不得开始 P16。完成定点测试、原兼容回归、TypeScript、Android lint/build并更新进度。

============================================================
P16 指令：专注时段 V1 与减少干扰
============================================================

前提：P15 Gate 已通过。

只执行 P16-00 至 P16-06。

重点：

- 复用现有 FocusSession、nextStartAt、taskExecutionPlan 和提醒预算；
- 新增唯一专注时段模型；
- 合并为单一 FocusAgenda，避免重复列表、重复通知和重复 FocusSession；
- 创建流程最多四步；
- 支持具体任务、成长区、自动选择；
- 支持一次、每天、工作日、指定星期；
- 到点支持开始、10 分钟后、今天跳过、重新安排；
- 专注进行页极简；
- 本轮只做“仅提醒 / 减少干扰”，不做强制锁机、VPN、Accessibility Service。

不得开始 P17。

============================================================
P17 指令：语义成长与连续主动开始
============================================================

前提：P16 Gate 已通过。

只执行 P17-00 至 P17-06。

重点：

- FocusSession 保存任务语义快照；
- 成长页完整承载成长物，象限不重复；
- 今日只显示投入、主动开始、成长区投入；
- 本周最多三个真实改善结果；
- 样本不足隐藏，不伪造趋势；
- 连续记录奖励“主动开始”，不要求完成全部任务；
- 每次只显示一条可执行建议；
- 用户端统一称“成长值”。

不得开始 P18。

============================================================
P18 指令：我的、偏好、隐私与可靠性
============================================================

前提：P17 Gate 已通过。

只执行 P18-01 至 P18-07。

重点：

- 我的节奏；
- 任务与象限规则；
- 提醒与专注保护；
- 外观与无障碍；
- 数据与隐私；
- 帮助与关于；
- 设置两层内可达；
- 权限按需申请；
- 备份合并继续阻断；
- 360×800、最大字体、深色、减少动态和大数据量收口。

完成后运行全部测试、TypeScript、Android lint/build，记录 APK 路径、大小、SHA-256。DEVICE/UTEST 与 iOS 未执行不得写 PASS。

============================================================
偏航纠正短句
============================================================

还在弹象限选择框：
普通触摸用户长按后必须直接拖动节点并松手保存；象限选择只保留为无障碍/更多替代。

还保留巨大关闭按钮：
删除“关闭任务面板”大按钮，使用真实拖动横杆、小型关闭图标、遮罩和系统返回。

Sheet 打开仍显示底部导航：
隐藏或完全禁用底部导航，且不得通过卸载首页丢失地图状态。

快速编辑仍有六个大按钮：
唯一主按钮是“先做 5 分钟”；完成/重新安排/需要帮助为次操作；复制/长期计划/移动/删除进入更多。

继续往象限堆内容：
象限只负责“现在做什么”；专注安排归专注，成长归成长，规则和数据归我的。

创建第二套日程：
专注时段必须与 taskExecutionPlan、nextStartAt 和 active focus 合并为 FocusAgenda。

伪造成长趋势：
样本不足就隐藏；只使用真实 FocusSession 快照、任务和修复记录。

备份合并：
P14-02B 仍为 BLOCKED_SPEC_GUARD，不得猜测实现。
