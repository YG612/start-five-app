# P18 DEVICE / UTEST 验收记录

状态：`PENDING_EXTERNAL`

原因：2026-08-17 执行 `adb devices -l`，连接设备数为 `0`。以下项目未写为 PASS。

## 安装包

- 路径：`android/app/build/outputs/apk/internal/app-internal.apk`
- 大小：`20,203,487` bytes
- SHA-256：`aeb2fd1f04cb98853ec6c0659e3609824510750cbbffae90847c8353e6b8e513`
- applicationId：`com.startfive.app`

## Android DEVICE

1. 执行 `adb install -r android/app/build/outputs/apk/internal/app-internal.apk`。
2. 清除应用数据后冷启动；确认直接进入象限、首屏只有一个主行动，且不自动弹出帮助或通知权限请求。
3. 在“我的”中于 10 秒内找到通知、外观、备份；逐项验证设置行在 fontScale 1.0 / 1.3 / 1.6 / 2.0 下不截断。
4. 开启 TalkBack；验证四个底部页面、象限清单、移动任务、设置 Sheet 均可等价操作，计时器不每秒播报。
5. 验证浅色、深色、跟随系统及减少动态；覆盖 360×800、393×852、412×915。
6. 创建每天 20:30、25 分钟的专注时段；再修改全局默认，确认已有时段不变、新时段使用新默认。
7. 验证到点通知的开始、10 分钟后、跳过；覆盖前台、后台、进程被杀及设备重启。
8. 开启“保持屏幕常亮”后开始 2 分钟专注；确认专注期间屏幕保持、结束或中断后恢复系统休眠策略。
9. 分别开关震动反馈和专注结束声音，完成专注后核对组合行为；中断不应播放完成反馈。
10. 验证专注后台恢复、App 内减少干扰、常驻通知和今日安排去重；不得声称阻断其他 App。
11. 创建备份，核对数据概览只显示数量、日期、占用；预览后安全替换可用，合并无执行按钮。
12. 删除全部数据：错误确认文本不可执行；输入“删除全部数据”后可执行；删除前“先创建备份”可用。

每项记录：设备型号、Android 版本、结果、截图/录屏路径、失败复现步骤。

## 首次用户 UTEST

找 5 名未看说明的目标用户，禁止诱导，依次完成：

1. 打开 App，指出现在应该点哪里。
2. 说出四个底部页面分别做什么。
3. 添加“周五交课程报告”。
4. 开始 5 分钟。
5. 安排今天 20:30 的 25 分钟专注。
6. 找到本周改善。
7. 修改默认提醒强度。
8. 找到备份入口。

逐人记录：完成/失败、首次犹豫位置、误点、操作时长、用户原话、是否需要解释。

## iOS

状态：`PENDING_MACOS_XCODE`。在 macOS/Xcode 验证编译、通知、前后台计时、VoiceOver、动态字体、深色、AppIcon / LaunchScreen、本地通知动作、常亮与完成反馈。
