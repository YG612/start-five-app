# Start Five P19：全局交互可靠性与发布收口

## 一、目标

项目路径：

`D:/CodexData/Workspaces/Codex/2026-07-20/android-ios-app-readme-md-prd/outputs/start-five`

主要基线：

- `docs/CODEX_P15R_P18_PROGRESS.md`
- `docs/CODEX_P15R_P18_UNIFIED_INTERACTION_AND_PAGE_EXPERIENCE_SPEC.md`
- `docs/P18_DEVICE_UTEST_ACCEPTANCE.md`

本阶段不增加新功能，不做全量视觉重设计，而是将现有功能收口到可真实使用、可退出、可恢复、不会丢数据的状态。

用户已经确认至少存在以下真实问题：

1. 原四象限任务长按拖动不符合持续布局模式要求。
2. 部分看起来可以下拉关闭的底部弹层，真机上无法下拉关闭。
3. 仍有多处交互细节显著影响使用体验。

完成标准不是“源码存在”和“Jest 通过”，而是：

- 用户知道如何进入、完成和退出；
- 关闭界面不会意外丢失输入；
- 一个操作不会重复提交；
- 失败后 UI 和持久化数据保持一致；
- 键盘、滚动、拖动、返回和弹层不互相抢占；
- Android 真机未验证时不得宣称发布完成。

---

## 二、工作边界

本阶段允许：

- 修复共享交互基础设施；
- 修复所有已确认的 P0、P1 问题；
- 对直接导致交互混乱的组件做有限拆分；
- 补充可靠性测试和验收文档；
- 修复真实存在的数据展示、状态恢复和幂等问题。

本阶段禁止：

- 新增产品模块；
- 启用备份合并；
- 全量改变品牌、颜色或导航结构；
- 为了文件变短而机械拆分组件；
- 引入新的全局状态管理框架；
- 没有证据地重写任务、专注、成长或备份领域模型；
- 处理 Gradle 10 升级等非用户阻断问题；
- 用延长 timeout、删除断言或伪造 DEVICE 结果换取通过。

保留工作区已有改动，不得执行破坏性 reset、checkout 或覆盖用户文件。

---

## 三、先核实当前工作区

先执行：

```bash
git status --short
git diff --stat
git log -5 --oneline

rg --files src/screens src/components src/app src/application src/data src/domain
rg -n "AppBottomSheet|<Modal|BackHandler|KeyboardAvoidingView|Keyboard|ScrollView|FlatList|SectionList|PanResponder|Gesture|onLongPress|onRequestClose|onDismiss|AppState|useFocusEffect" src
rg -n "QuadrantHomeScreen" src tests
```

读取：

- `package.json`
- 应用导航和根组件；
- `src/screens/QuadrantHomeScreen.tsx`
- `src/components/AppBottomSheet.tsx`
- 所有 `src/screens` 文件；
- 任务、专注日程、专注会话、设置、通知和备份运行时；
- 当前相关测试。

先确认此前要求的 P15R-04B 持续任务布局模式是否已经真实落地：

- 1000ms 长按选中；
- 松手后仍保持选中；
- 同一手势或第二次触碰均可拖动；
- 象限内位置持久化；
- 跨象限更新；
- 点击外部退出；
- 减少动态兼容。

若未完整落地，先完成该要求，再进入 P19。不得同时保留两套互相竞争的任务拖动实现。

---

## 四、P19-00：建立完整交互审计

新增：

`docs/P19_RELEASE_CONVERGENCE_AUDIT.md`

必须从真实代码生成清单，不能只按进度文档猜测。

清点所有：

- 四个底部主页面；
- 全屏二级页面；
- `AppBottomSheet`；
- 原生或 React Native `Modal`；
- 自定义绝对定位遮罩；
- 确认框；
- 展开区域；
- 键盘表单；
- 系统权限入口；
- 通知点击和通知动作入口；
- 后台恢复入口。

每个界面记录：

- UI ID 和真实组件；
- 从哪里打开；
- 页面、Sheet、Dialog 或菜单类型；
- 可见退出方式；
- Android 返回行为；
- 是否可滚动；
- 是否有键盘；
- 是否存在未保存状态；
- 点击遮罩行为；
- 下拉行为；
- 页面切换行为；
- App 后台或组件卸载行为；
- 无障碍焦点行为；
- 当前自动测试；
- 当前真机证据；
- 问题级别 P0、P1 或 P2。

严重度定义：

- **P0**：崩溃、界面死路、核心流程无法完成、数据丢失、重复执行、错误删除或恢复。
- **P1**：返回、键盘、滚动、拖动、遮罩冲突；核心内容不可识别；重要数据展示口径不一致；无障碍无法完成核心流程。
- **P2**：不阻断任务的视觉和动效细节。

发现大量问题时，先按共享根因归组，不得为每个页面分别添加一套关闭或手势代码。

审计后立即继续修复 P0/P1，不要停留在只写文档。

---

## 五、P19-01：统一弹层、返回、键盘和遮罩

### 5.1 界面类型必须区分

不得给所有界面统一增加下拉关闭：

- Bottom Sheet：支持下拉、遮罩、系统返回和可见关闭入口。
- 全屏二级页面：使用顶部返回、Android 系统返回和 iOS 标准返回。
- Dialog：明确确认与取消；点击外部是否关闭由危险程度决定。
- 有未保存输入的表单：关闭前保存草稿、完成写入或明确询问，不得静默丢数据。

### 5.2 AppBottomSheet 单一关闭契约

所有 Bottom Sheet 必须通过同一个关闭状态机，不得由业务页面各自实现不同手势。

建议接口：

```ts
type SheetCloseReason =
  | 'swipe'
  | 'backdrop'
  | 'back'
  | 'closeButton'
  | 'navigation'
  | 'programmatic';

type SheetDismissPolicy =
  | 'free'
  | 'flushBeforeClose'
  | 'confirmDirty'
  | 'cancelOnly';
```

必须满足：

1. 顶部始终有清晰拖拽把手。
2. 必要时有可见关闭按钮。
3. 从把手区域下拉可关闭。
4. Sheet 内有滚动内容时：
   - 内容未到顶部，优先滚动；
   - 内容已到顶部且继续向下，转为拖动 Sheet。
5. 达到距离或速度阈值后关闭，未达到时平滑回弹。
6. 点击遮罩按 dismiss policy 处理。
7. Android 返回键按 dismiss policy 处理。
8. 键盘显示时先正确收起键盘，再完成关闭或继续拖动。
9. 关闭回调只执行一次。
10. 动画结束、组件卸载和异常取消时清理手势、监听器和动画。
11. 最上层 Sheet 打开时，底部导航和下层页面不得继续接收点击。
12. Sheet 关闭后焦点返回原触发控件。
13. 开启减少动态时缩短或取消非必要动画。
14. Sheet 高度、底部安全区和键盘高度不得使用固定设备像素推算。

优先复用项目已有依赖。若现有依赖中已有成熟 Sheet 实现，统一使用；若没有，修复当前组件，不得未经论证增加大型原生依赖。

### 5.3 单一返回优先级

检查并收口所有分散的 `BackHandler`。

应用返回优先级统一为：

```text
系统键盘
→ 最上层确认框
→ 最上层 Bottom Sheet
→ 四象限任务布局模式
→ 全屏二级页面
→ 当前主页面 / App
```

要求：

- 一次返回只处理一层；
- 不得重复注册导致一次返回关闭两层；
- Sheet 打开时不得直接退出 App；
- 布局模式中第一次返回只退出布局模式；
- 页面失焦和组件卸载后移除监听；
- iOS 不给普通 Sheet 错误增加页面边缘返回。

---

## 六、P19-02：按核心用户流程检查状态与数据可靠性

不要按文件逐个修改。按用户完整任务链检查。

### 6.1 任务链

覆盖：

- 快速创建；
- 编辑；
- 取消和退出；
- 删除；
- 进度更新；
- 任务整理；
- 象限内拖动；
- 跨象限拖动；
- App 后台后恢复；
- 页面切换后恢复。

验证：

- 长标题、emoji、空标题和重复标题；
- 快速连续点击；
- 保存失败；
- 拖动提交失败；
- 任务在操作期间被删除或更新；
- 同一个松手事件不会写入两次；
- 失败后保留输入并恢复真实 repository 状态；
- 任务 ID、完成状态和成长事件不会因拖动被错误改变。

当前草稿若只保存在内存中，实际复现以下场景：

1. 输入任务但未提交；
2. App 进入后台；
3. 模拟进程被杀；
4. 重新打开 App。

若确认用户输入丢失，增加最小化、版本化的本地草稿存储：

- 创建和编辑草稿按明确 ID 隔离；
- 输入后节流保存；
- 成功提交或明确放弃后删除；
- 旧草稿有过期策略；
- 不修改正式任务 schema；
- 坏草稿不得阻止 App 启动。

### 6.2 专注链

覆盖：

- 开始；
- 同任务恢复；
- 不同任务冲突；
- 暂停或中断；
- 提前结束；
- 正常完成；
- 完成后四种选择；
- 后台和通知恢复；
- App 被杀后重新进入。

必须保持：

- 同时只有一个活动专注；
- 结束动作幂等；
- 通知动作和 App 内动作不会重复结算；
- 短时会话、有效会话、次数和分钟数使用明确口径；
- “今日专注次数”和“今日专注分钟”若使用不同纳入规则，界面必须明确解释；
- 不允许出现用户刚完成有效专注，但汇总仍因缓存或日期口径显示错误；
- 日期跨零点、系统时区变化和后台恢复后数据重新计算正确。

特别追查截图中类似：

`今日专注：2 次 / 0 分钟`

确认这是产品规则、取整显示还是聚合错误。不能只修改文字掩盖数据问题。

### 6.3 日程和通知链

覆盖：

- 创建、编辑、暂停和删除日程；
- 权限允许、拒绝和永久拒绝；
- 通知点击；
- 跳过、延后和开始动作；
- App 前台、后台和被终止；
- 系统重启；
- 日期、时区和夏令时变化；
- 同一通知动作重复回传。

要求：

- 调度幂等；
- 删除或暂停后旧通知不继续触发；
- 权限拒绝后给出可理解状态和设置入口；
- 通知点击进入正确任务或日程；
- 无设备时如实保留 `PENDING_EXTERNAL`。

### 6.4 设置和数据链

覆盖：

- 专注时长和工作日偏好；
- 外观、减少动态和屏幕阅读器设置；
- 通知、声音、震动和常亮；
- 备份导出；
- 安全替换恢复；
- 全量清除；
- 失败恢复。

要求：

- 偏好修改后仅影响规格规定的对象；
- 撤销后真实存储和页面一致；
- 恢复备份必须先完整校验，再原子替换；
- 恢复失败不破坏旧数据；
- 清除操作不可被快速连点重复执行；
- 保留已有旧版本备份兼容；
- 本阶段不得开放备份合并。

---

## 七、P19-03：可理解性、适配和无障碍

只修复影响使用的 P0/P1，不进行全量美术重做。

### 7.1 信息与操作层级

检查所有核心页面：

- 一个区域只能有一个明显主操作；
- 次级和低频操作不得与主操作视觉等权；
- 保存中、成功、失败和禁用状态必须明显不同；
- 可点击元素不能看起来像普通说明文字；
- 不显示重复标题和重复状态；
- 错误提示说明用户下一步；
- 失败后不清空用户输入；
- 同一页面同时只显示一个主要错误提示。

专注完成 Sheet 中四个动作需要保持业务能力，但必须形成明确的主次层级，避免四个等权大按钮要求用户每次重新阅读。

### 7.2 四象限任务可识别性

要求：

- 视觉卡片可以紧凑，但实际触摸区域不得小于 48dp；
- 标题至少提供稳定的两行或合理宽度；
- 选中和拖动预览显示完整标题；
- 无障碍标签读取完整标题、象限和任务状态；
- 空白很多时不得仍把任务压缩成无法辨认的小块；
- 相邻任务的 hitSlop 不得重叠到造成误触；
- 选中、拖动和普通状态清晰区分。

### 7.3 尺寸、字体和安全区

至少检查：

- 320dp、360dp、412dp 宽度；
- 普通字体和系统最大字体；
- 长中文标题、英文标题和混合文本；
- 顶部状态栏；
- 底部手势区；
- 键盘打开和关闭；
- Sheet 打开时的底部导航；
- 页面滚动到顶部和底部；
- 横竖屏变化时是否安全，若产品明确只支持竖屏则验证锁定行为。

不得出现：

- 主按钮被键盘遮挡；
- 文字重叠或不可点击；
- Sheet 超出屏幕无法关闭；
- 内容被状态栏或底部手势条裁切；
- 透明遮罩关闭后仍拦截点击。

### 7.4 无障碍

检查：

- TalkBack / VoiceOver 阅读顺序；
- Sheet 打开后的焦点进入；
- Sheet 关闭后的焦点恢复；
- 按钮名称和状态；
- 任务选中状态；
- 拖动的非手势替代动作；
- 最大字体；
- 减少动态；
- 对比度；
- 开关和单选项的可访问状态。

自由拖动不能成为唯一改变任务象限的方式，必须保留可访问的整理或移动入口。

---

## 八、控制架构风险，但不要全量重写

统计并报告：

- `QuadrantHomeScreen.tsx` 实际行数；
- 本地 state 数量；
- effect 和监听器数量；
- Sheet / overlay 数量；
- 同时承担的页面职责；
- 与任务、专注、成长、设置和通知的直接依赖。

不要仅因为文件大就拆分。

只有在审计确认职责冲突时，进行有限抽取：

- 四象限地图及拖动；
- 顶层 Overlay 协调；
- 专注主页展示；
- 成长主页展示；
- 我的页设置展示。

要求：

- 页面协调器负责组合和导航，不直接实现所有手势、动画和业务写入；
- 业务写入继续通过现有 application/service 层；
- 不引入新的全局状态框架；
- 每次抽取保持行为测试先行；
- 不同时重写领域模型和 UI 架构。

如果一个问题可以通过修复共享组件解决，不得在十个页面分别增加补丁。

---

## 九、测试

新增：

`tests/p19-release-convergence`

至少覆盖：

### Sheet 和返回

- 下拉达到阈值关闭；
- 未达到阈值回弹；
- 内部滚动未到顶部时不误关；
- 滚动顶部继续下拉关闭；
- 遮罩关闭；
- Android 返回关闭；
- 脏表单关闭策略；
- 键盘打开时关闭；
- `onDismiss` 只触发一次；
- 多层 overlay 只关闭最上层；
- 底部导航在 modal 打开时不可点击；
- 组件卸载后监听清理。

### 核心流程

- 创建、编辑、取消和失败保留输入；
- 任务进度保存幂等；
- 同象限和跨象限拖动；
- 拖动失败回滚；
- 专注开始、恢复、完成和重复事件；
- 次数与分钟聚合口径；
- 日程通知动作幂等；
- 偏好修改和撤销；
- 备份替换失败不破坏旧数据；
- 全量清除防重复；
- App 后台和恢复后的状态一致。

### 适配和无障碍

- 最大字体下核心按钮仍可操作；
- 任务触摸区域；
- 完整 accessibility label；
- Sheet selected / expanded 等状态；
- 减少动态不运行循环动画；
- 关闭后焦点恢复。

不要只做快照和文字存在性测试。优先测试状态转换、调用次数、持久化结果和失败回滚。

---

## 十、源码与构建门禁

使用项目当前真实脚本执行：

1. P19 定点测试；
2. P15R—P19 相关回归；
3. 全部可执行测试；
4. TypeScript：
   ```bash
   tsc --noEmit
   ```
5. Android：
   ```bash
   ./gradlew :app:lintInternal
   ./gradlew :app:assembleInternal
   ```

Windows 使用对应的 `gradlew.bat`。

要求：

- 报告完整回归和子集回归时分别命名，不得混为一个测试总数；
- `package.json` 没有 JavaScript lint 时如实记录；
- 不得删除旧测试或降低断言；
- 构建完成后重新计算 APK 大小和 SHA-256；
- 旧 APK 的 hash 不得当作新构建结果。

---

## 十一、真机和用户验收

执行：

```bash
adb devices -l
```

有 Android 设备时逐项验证：

- 每一个 Sheet 的把手、下拉、遮罩、关闭按钮和返回；
- 内部滚动与 Sheet 下拉衔接；
- 键盘遮挡和收起；
- 底部导航层级；
- 四象限选中和拖动；
- 页面滚动与任务拖动；
- 专注后台和通知恢复；
- 通知跳过、延后、开始；
- 最大字体；
- TalkBack；
- 减少动态；
- 备份、恢复和清除；
- 快速连点；
- App 后台、杀进程和重新进入。

没有设备时：

- DEVICE 继续标记为 `PENDING_EXTERNAL`；
- 不得写“已解决真机问题”；
- 输出可直接执行的真机验收表。

iOS 没有 macOS/Xcode 时继续标记：

`PENDING_MACOS_XCODE`

首次用户测试至少观察用户在无指导情况下完成：

1. 创建并编辑任务；
2. 调整任务象限和位置；
3. 开始并结束一次专注；
4. 安排下一次；
5. 打开并关闭主要 Sheet；
6. 修改设置；
7. 找到备份入口；
8. 返回首页。

记录卡住、误点、找不到出口和需要解释的位置。开发者提示后才完成，不得记为通过。

---

## 十二、进度状态治理

保留历史记录，但在当前状态中明确重开：

- P15R-01：`REOPENED_USER_FAIL`，原因是下拉关闭真机失败；
- P15R-04：按 P15R-04B 实际实现和验证状态更新；
- P15R-08：`AUDIT_REQUIRED`，直到所有 Sheet 清点完成；
- 所有仅通过源码和自动测试的项目不得写最终 `DONE`。

统一状态：

- `AUDIT_CONFIRMED`
- `REOPENED_USER_FAIL`
- `DONE_SOURCE`
- `DONE_AUTO`
- `DEVICE_PASS_ANDROID`
- `DEVICE_PASS_IOS`
- `UTEST_PASS`
- `RELEASE_READY`
- `PENDING_EXTERNAL`

更新：

`docs/CODEX_P15R_P18_PROGRESS.md`

并修正：

- 顶部更新时间；
- 当前完整测试总数；
- 子集回归总数；
- 当前 APK 信息；
- 当前 schema 版本；
- 真实外部待验项。

---

## 十三、结束条件

本轮只有同时满足以下条件才可结束源码阶段：

1. 所有审计发现的 P0 已修复并有回归测试。
2. 核心用户流程中的 P1 已修复。
3. 每个临时界面都有明确、有效且符合类型的退出方式。
4. 不存在已知输入丢失、重复提交或失败后状态错位。
5. Sheet、返回、键盘、滚动和拖动使用统一契约。
6. 全量自动测试、TypeScript、Android lint 和 APK 构建通过。
7. 所有 P2 被集中记录，而不是顺手散改。
8. 无设备时如实停在 `DONE_AUTO / PENDING_EXTERNAL`。

`RELEASE_READY` 必须建立在 Android 真机、必要的 iOS 验证和首次用户测试证据上，不能仅依据自动测试。

最终只输出：

1. 审计发现的 P0 / P1 / P2 数量；
2. 共同根因；
3. 已修复内容；
4. 修改文件及职责；
5. 测试与构建结果；
6. APK 信息；
7. 真机和用户测试状态；
8. 尚未完成但不属于本轮阻断的问题。

不要输出冗长过程日志，不要用“理论上”“应该可以”代替验证结果。