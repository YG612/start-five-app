# GAP-P0-11 — 首次激活：从空白到第一轮专注 tests-first contract

## 范围

本切片只补齐全新安装的首次激活闭环：一个页面内输入第一项并直接开始 5 分钟，或明确跳过后进入既有工作台。它复用现有任务、推荐、专注、复盘与持久化服务，不另造演示任务、临时专注或第二套任务状态。

为不改变既有组合测试的启动前提，`StartFiveAppDependencies` 可新增可选的公开配置 `public.firstActivation`；只有显式启用时才进入本合同。测试只经 `createStartFiveApp`、返回的 `AppRoot`/`composition`、公开生命周期服务和可见 UI 观察结果。

## 固定语义

- 全新且可正常读取的空后端只显示单屏“开始我的第一项”，包含任务标题输入、“开始5分钟”和“暂时跳过”。首次页面不得请求通知权限。
- 提交非空标题必须沿既有公开任务生命周期创建一个稳定任务，并精确启动该任务的一轮 5 分钟专注；不得先启动推荐中的另一个任务，也不得因双击、重渲染或字节重启创建第二份。
- 首次激活完成标记只能在“任务创建与精确专注启动均已收敛”之后提交。生产实现必须采用稳定操作 ID 和可恢复的单向状态；若任务创建后标记写入失败，重试必须复用同一任务并继续收敛，不能复制任务。当前公开依赖没有可确定命中该单点的失败注入缝，因此此不变量保留为静态合同，不在测试里伪造私有 key 或写序号。
- “暂时跳过”是明确、持久、单向的完成决定；字节重启后不得再次出现首次激活。
- 升级安装只要经公开生命周期可观察到既有任务、专注或复盘数据，就不得显示首次激活，也不得回写、迁移或重播既有业务操作。
- 首次状态读取失败或标记格式损坏必须 fail-closed：进入工作台或可重试错误态，但绝不把未知状态当作全新安装。恢复读取成功之前不得创建任务或请求通知权限。

## 公开契约（恰好三个 AppRoot/UI 测试）

1. 全新空后端展示唯一首次激活单屏，标题输入与跳过入口可达，通知假对象的权限请求保持为零。
2. 输入 A 并提交后，只存在一个稳定的 A，A 为 `in_progress` 且可见专注精确指向 A；字节重启恢复同一专注并且不再显示首次激活，持久化语义不漂移。
3. 跳过在字节重启后仍不再显示；由公开任务→专注→复盘流程形成的升级数据首次启用时直接进入既有产品流程；通用首次读取失败进入可见错误态且不展示首次激活。

## 测试边界

不得读取私有 React context/provider、持久化 key/envelope、原始读写次数或内部协调器；不得使用 sleep、fake timer、私有模块 mock 或通过写入未知 key 制造“旧数据”。允许公开内存后端的字节重启/稳定快照、一次通用下一读失败、结构化时钟、可见 UI 和 `composition.service.getState()`。

## Red-run policy

仅运行本目录 3 条测试一次，并运行 TypeScript 一次。当前生产尚未接线 `public.firstActivation`，合法红应为缺少“开始我的第一项”或 fail-closed 启动态；不得运行旧回归、quality gate、native build 或重复红测。

## 冻结前序

- GAP-P0-10R1 candidate self：由父流程冻结记录提供。

## 三项候选记录

1. 新测试命令与结果：待单次隔离运行后填写。
2. TypeScript 结果：待单次运行后填写。
3. 最小生产缺口：待红证据后填写。
