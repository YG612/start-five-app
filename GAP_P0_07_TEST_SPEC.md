# GAP-P0-07 测试规范：专注复盘与可信积分回执

## 1. 切片边界

本切片只锁定一条成熟消费级闭环：`专注结束 -> 即时复盘 -> 持久结算回执 -> 返回任务工作台`。

PRD 对专注后的正反馈和成长感有更宽的产品方向；但当前公开领域事实只有四象限任务完成积分（Q1=35、Q2=45、Q3=15、Q4=5）与持久 `totalScore`。因此本切片只交付可验证的“任务积分回执”：

- 选择“有进展”时记录复盘，任务不自动完成，本次积分为 `0`；
- 选择“完成任务”时复用既有任务完成积分，且只奖励一次；
- 回执说明积分原因，并显示当天已结算专注的次数与分钟；
- 等级、连续天数、徽章、成长中心、额外专注奖励均明确不在本切片范围内，不能用占位数字冒充完整成长模型。

## 2. 公共可测面

验收只从 `createStartFiveApp(...).AppRoot` 和公开 UI 驱动，使用公开的 `storageBackend`、ISO clock、ID generator 与结构化 `focusRuntimeClock.subscribe` seam。禁止 sleep、fake timer、process listener、私有 Provider/context、私有 storage key、envelope 解码以及 raw set 次数断言。

测试 backend 只实现公开 KV 接口。重启仅复制全部 KV 原始字符串；幂等性只比较排序后的完整 backend 字节和公开投影。

生产实现预计需要独立的 post-focus review application/domain/repository 能力，用于持久 pending review、稳定 operationId、可恢复结算 journal/saga 与 receipt。具体 key、schema、事务拆分和恢复算法不属于锁定面。Focus 与 Task 位于不同持久仓储，因此测试不要求跨 key 瞬时原子，只要求失败/重试/重启后最终收敛到一个稳定回执，任务与积分不重复。

## 3. 公共 UI 文案

### 3.1 复盘页

- 标题：`专注复盘`
- 事实：`复盘任务：<标题>`、`实际专注：<秒数>秒`
- 状态：`结束状态：自然完成` 或 `结束状态：提前结束`
- 单选项：`有进展`、`完成任务`
- 可选输入：`复盘备注`
- 提交：`确认结算`

专注结束不得默认完成任务；用户必须显式选择“完成任务”。

### 3.2 回执页

- 标题：`专注回执`
- 任务：`回执任务：<标题>`
- 积分：`本次积分：<数字>`
- 原因：progress 为 `积分原因：记录专注进展（任务未完成）`；完成 Q2 任务为 `积分原因：完成成长区任务`
- 今日统计：`今日专注：1次 / 5分钟`（自然完成五分钟旅程）
- 返回：`返回任务工作台`
- 返回刷新失败：`工作台刷新失败` 与 `重试刷新工作台`

## 4. 精确三条旅程

### 4.1 自然结束、提交前重启、progress 回执

公开 UI 启动一项含小步的任务；ISO clock 与 runtime clock 前进至五分钟截止点。验收复盘任务、`300秒` 与自然完成状态。提交前销毁 composition，只复制 backend 全字节创建新 AppRoot，仍必须恢复同一待复盘。

选择“有进展”、填写短备注并确认。验收任务仍为 `in_progress`、本次 `0` 分、原因明确、今日 `1次 / 5分钟`。再次 byte-restart 后恢复同一回执，渲染恢复不改 backend 字节。

### 4.2 公开提前结束、显式完成、双提交与重启幂等

公开 UI 启动 Q2 任务，在专注中完成其小步；两分钟时按公开 `中断专注`。验收复盘显示 `120秒`、提前结束，且结算前任务仍为 `in_progress`。

显式选择“完成任务”，对同一个 `确认结算` 控件连续触发两次。验收只有一个稳定回执，Q2 任务完成且总积分恰为 `45`。byte-restart 后回执、任务状态与总积分保持一致，完整 backend 字节不因恢复而变化。

### 4.3 回执返回时刷新失败只重试投影

完成一项 Q2 主任务并保留一个 Q4 后备任务。回执出现后，测试 backend 仅按“下一次公开 KV read”注入一次失败，不识别 key 或内容。按 `返回任务工作台` 后必须保留已结算回执语义，显示刷新失败和显式重试入口。

按 `重试刷新工作台` 前后完整 backend 字节不变；成功后主任务从活动象限移除、后备任务成为今日推荐，公开状态仍只有一次 `45` 分奖励。该旅程证明刷新重试不会重放结算。

## 5. 执行纪律

- 测试数固定为 `3 it`，不扩套件。
- 候选阶段只运行本目录一次与 `tsc --noEmit`。
- fixture 错误立即停；合法红后冻结候选，独立审查通过前不得授权生产实现。

