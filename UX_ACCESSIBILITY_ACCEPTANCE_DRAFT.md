# UX and accessibility acceptance draft

> **NON-BINDING / UI NOT INSPECTED**
>
> 本文是产品体验与无障碍验收草案，不是已实现声明、视觉审查结论、测试报告、
> 设计系统规范或发布批准。当前未读取项目代码、设计稿、导航结构、组件库、主题
> token、原生工程或现有自动化测试。所有标为 `TBD` 的内容必须在检查真实 UI、
> 代码与产品决策后确认。

## 1. 草案适用范围

本文仅基于以下产品上下文建立验收框架：

- 核心心理入口是“先做 5 分钟”；
- 用户会管理四象限中的多项任务；
- FocusSession 支持 2 / 5 / 15 / 25 / 50 分钟；
- 产品包含任务提醒与延迟诊断；
- Android 与 iOS 都应覆盖核心旅程和无障碍使用方式。

本文不假设当前页面名称、按钮名称、导航层级、任务字段、通知库或状态管理方案。
示例中文文案用于表达语气目标，最终文本必须经过产品、设计和本地化审查。

## 2. 优先级与证据类型

### 优先级

- **P0**：发布阻断。失败会使核心旅程不可完成、造成数据/隐私风险、误导用户，
  或让主要辅助技术用户无法使用。
- **P1**：发布前应完成。失败不会立即破坏核心功能，但会明显降低可理解性、效率、
  包容性或跨设备质量。

### 建议证据

- **组件**：组件/页面级自动化，验证语义树、状态、文案、事件与布局约束。
- **E2E**：使用项目既有 E2E 工具验证真实导航、持久化、恢复和平台交互。
- **手工**：在真实 Android/iOS 设备与系统辅助功能下验证体验。
- **视觉**：截图/视觉回归覆盖字体缩放、深浅色、小屏、横屏和错误态。
- **性能**：在已定义参考设备和数据规模下记录可复现的 trace/指标。

自动化工具名称、参考设备与系统最低版本均为 `TBD`；不得仅为满足本文而引入新依赖。

## 3. 全局体验原则

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| UX-GEN-P0-01 | P0 | 任一核心旅程都不存在只能依赖颜色、拖拽、长按、复杂手势、声音或动画才能完成的步骤。 | 组件 + E2E + 手工 |
| UX-GEN-P0-02 | P0 | 所有会修改数据的主操作有明确结果；重复点击不会创建重复任务、重复 FocusSession、重复提醒或重复诊断。 | 组件 + E2E |
| UX-GEN-P0-03 | P0 | 页面不会在没有说明的情况下丢失用户已输入内容；离开、取消或破坏性操作遵循经确认的保存/确认策略。 | 组件 + E2E |
| UX-GEN-P0-04 | P0 | 成功、失败、不可用和进行中状态不只通过颜色表达，并向屏幕阅读器提供等价信息。 | 组件 + 手工 |
| UX-GEN-P0-05 | P0 | 系统返回、手势返回、页面内返回和取消不会绕过数据保护或把用户困在循环导航中。 | E2E + 手工 |
| UX-GEN-P1-01 | P1 | 每屏只有一个清晰的主要动作；次要动作不与主要动作竞争，也不误导为破坏性动作。 | 设计审查 + 手工 |
| UX-GEN-P1-02 | P1 | 用户始终知道当前任务、象限、FocusSession 状态及下一步，不需要记忆上一屏信息。 | 组件 + 手工 |

## 4. 核心用户旅程

### 4.1 首次进入与空态

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| UX-ONB-P0-01 | P0 | 首次进入在无权限、无任务、无网络时仍可理解并继续；通知权限不是进入产品的强制门槛。 | E2E + 手工 |
| UX-ONB-P0-02 | P0 | 空态明确说明“可以先建一个很小的任务”，提供一个可聚焦的主要入口；不会显示无意义的空图表或错误态。 | 组件 + 视觉 + 手工 |
| UX-ONB-P0-03 | P0 | VoiceOver/TalkBack 首个焦点落在页面标题或空态说明，随后到主要动作；不会先读装饰图。 | 组件 + 手工 |
| UX-ONB-P1-01 | P1 | 首次说明可跳过且以后可再次查看，不使用连续弹窗迫使用户做完整设置。 | E2E + 手工 |
| UX-ONB-P1-02 | P1 | 空态文案在 200% 文本缩放和最小支持屏幕上不截断，主要动作无需横向滚动即可到达。 | 视觉 + 手工 |

### 4.2 创建任务

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| UX-CREATE-P0-01 | P0 | 用户仅填写最低必需信息即可创建任务；必填项、可选项和错误原因清晰标注。 | 组件 + E2E |
| UX-CREATE-P0-02 | P0 | 提交无效时，焦点移动到错误摘要或首个错误字段；错误与字段建立程序化关联，已填的其他内容保留。 | 组件 + 手工 |
| UX-CREATE-P0-03 | P0 | 提交中防止重复提交，并同时提供可见与可读的进行中状态；失败后恢复可编辑状态。 | 组件 + E2E |
| UX-CREATE-P0-04 | P0 | 日期/时间输入有文字标签和可解析值，不只用图标或占位符；清除时间也有明确按钮与确认结果。 | 组件 + 手工 |
| UX-CREATE-P1-01 | P1 | 键盘类型、下一项/完成动作与字段匹配；键盘不会遮挡当前字段、错误或提交按钮。 | 手工 + E2E |
| UX-CREATE-P1-02 | P1 | 返回列表后，新任务的插入位置可预测；屏幕阅读器收到简短成功反馈但不会被重复播报。 | E2E + 手工 |

### 4.3 定义“第一步”

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| UX-STEP-P0-01 | P0 | “第一步”被解释为可以马上开始的具体动作，而不是抽象目标；跳过不会阻止保存任务。 | 组件 + 手工 |
| UX-STEP-P0-02 | P0 | 建议不会自动覆盖用户标题、描述、估时、排期或既有第一步；采用建议前展示变化并由用户确认。 | 组件 + E2E |
| UX-STEP-P0-03 | P0 | 输入、清除和替换第一步均可通过触控、键盘和屏幕阅读器完成。 | 组件 + 手工 |
| UX-STEP-P1-01 | P1 | 示例短且中性，例如“打开文档并写下标题”，不暗示用户缺乏自律。 | 文案审查 |

### 4.4 四象限与多任务

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| UX-QUAD-P0-01 | P0 | 四象限同时使用文字名称/语义与视觉样式；重要/紧急状态不只依赖红、黄、绿等颜色。 | 组件 + 视觉 + 手工 |
| UX-QUAD-P0-02 | P0 | 每个任务项向辅助技术提供标题、象限/重要紧急状态、完成状态和可用动作；装饰信息不重复朗读。 | 组件 + 手工 |
| UX-QUAD-P0-03 | P0 | 若支持拖拽换象限，必须提供“移动到…”菜单或等价单指/键盘操作；取消拖拽不改变数据。 | E2E + 手工 |
| UX-QUAD-P0-04 | P0 | 多任务滚动时不会丢失焦点或错误激活相邻任务；任务操作菜单与所属任务建立清晰语义。 | 组件 + 手工 |
| UX-QUAD-P0-05 | P0 | 过滤、排序或折叠后，结果数量与当前条件可见且可读；清除条件的入口可聚焦。 | 组件 + E2E |
| UX-QUAD-P1-01 | P1 | 小屏和 200% 文本缩放时允许四象限转为列表/分段视图，不强迫二维滚动阅读。 | 视觉 + 手工 |
| UX-QUAD-P1-02 | P1 | 大列表保留可感知的加载/更新位置，返回详情后尽量恢复到原任务附近。 | E2E + 性能 |

### 4.5 开始 FocusSession

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| UX-FOCUS-P0-01 | P0 | 2 / 5 / 15 / 25 / 50 分钟选项均有完整可读名称、选中状态和足够触控区域；选择不只由颜色表示。 | 组件 + 手工 |
| UX-FOCUS-P0-02 | P0 | 开始前明确关联任务、第一步与时长；没有任务时的产品规则为 `TBD`，但 UI 不得静默关联错误任务。 | 组件 + E2E |
| UX-FOCUS-P0-03 | P0 | “先做 5 分钟”是鼓励性建议而非默认强迫；用户可选择其他时长并知道如何返回修改。 | 组件 + 文案审查 |
| UX-FOCUS-P0-04 | P0 | 开始动作只创建一个 Session；按钮进入禁用/进行中状态，失败后恢复且说明未开始。 | 组件 + E2E |
| UX-FOCUS-P0-05 | P0 | Focus 界面进入后焦点落在会话标题/状态；倒计时不会每秒打断 VoiceOver/TalkBack。 | 组件 + 手工 |
| UX-FOCUS-P1-01 | P1 | 最常用时长易于触达，但不会隐藏其余时长；横向列表若存在，屏幕阅读器可发现全部选项。 | 手工 + 视觉 |

### 4.6 中断、暂停（若支持）与完成

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| UX-END-P0-01 | P0 | 中断、放弃、完成等概念使用精确文案；不会把“暂时停下”误写为任务失败。 | 文案审查 + 组件 |
| UX-END-P0-02 | P0 | 破坏性终止操作有清晰确认或可撤销机制（最终策略 `TBD`），默认焦点不落在破坏性按钮。 | 组件 + E2E + 手工 |
| UX-END-P0-03 | P0 | 会话完成只记录一次；前后台切换、锁屏、系统时钟变化或重复点击不会重复结算。 | E2E + 组件 |
| UX-END-P0-04 | P0 | 完成反馈同时说明结果与下一步，例如返回任务、继续或休息；不会用闪烁、强烈震动或不可关闭动画强迫庆祝。 | 组件 + 手工 |
| UX-END-P0-05 | P0 | 中断原因/延迟诊断可以跳过；跳过不惩罚用户、不阻塞任务或会话数据保存。 | E2E + 文案审查 |
| UX-END-P1-01 | P1 | Reduce Motion 开启时，完成/过渡效果移除位移、缩放、弹跳和持续运动，保留静态或轻微淡入反馈。 | 组件 + 手工 |

### 4.7 重排、提醒与延迟诊断

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| UX-REPLAN-P0-01 | P0 | 重排前显示原时间和新时间；保存后明确说明提醒是否会更新，失败则原排期仍可确认。 | 组件 + E2E |
| UX-REPLAN-P0-02 | P0 | 提醒权限被拒绝时，任务仍可保存/重排；UI 说明如何在系统设置中开启，但不循环弹权限框。 | E2E + 手工 |
| UX-REPLAN-P0-03 | P0 | 延迟诊断使用中性问题“是什么让开始变得困难？”，原因选择有文字标签且允许“其他/跳过”。 | 组件 + 文案审查 |
| UX-REPLAN-P0-04 | P0 | 诊断建议第一步、估时或重排时，默认只形成建议；未确认前不更改用户任务数据。 | 组件 + E2E |
| UX-REPLAN-P0-05 | P0 | 诊断自由文本被标示为私密信息；不会出现在通知、无关统计、日志提示或其他任务的界面。 | E2E + 隐私审查 |
| UX-REPLAN-P1-01 | P1 | 重复延迟提示有频率控制和关闭方式，不在每次打开应用时制造压力。具体频率为 `TBD`。 | E2E + 产品审查 |

### 4.8 错误与数据恢复

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| UX-RECOVER-P0-01 | P0 | 可恢复错误说明“发生了什么、数据是否安全、下一步是什么”；技术堆栈、原始数据或密钥不展示给用户。 | 组件 + 安全审查 |
| UX-RECOVER-P0-02 | P0 | 数据损坏/版本不支持时不静默清空；提供符合产品恢复合同的查看、重试、恢复或导出入口。实际能力为 `TBD`。 | E2E + 手工 |
| UX-RECOVER-P0-03 | P0 | 恢复操作显示目标与来源、避免覆盖非空数据，并在失败时保留原数据和备份。 | E2E |
| UX-RECOVER-P0-04 | P0 | 错误出现后焦点移动到错误标题/对话框；关闭后回到触发控件或合理下一位置。 | 组件 + 手工 |
| UX-RECOVER-P1-01 | P1 | 离线、权限、存储和未知错误使用不同可行动文案，不统一显示“出了点问题”。 | 组件 + 文案审查 |

## 5. 中文文案与心理安全

### 文案原则

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| COPY-SAFE-P0-01 | P0 | 不使用羞辱、归因人格或绝对化词语描述延迟，例如“你又拖延了”“你失败了”“懒惰”“必须自律”。 | 文案清单审查 |
| COPY-SAFE-P0-02 | P0 | 延迟与中断被描述为可理解、可调整的情境，而不是道德失败。 | 文案清单审查 |
| COPY-SAFE-P0-03 | P0 | 诊断、提醒和庆祝文案不公开或推断心理健康结论，不制造恐惧、亏欠感或连续打卡压力。 | 产品 + 隐私审查 |
| COPY-SAFE-P0-04 | P0 | 错误文案不责怪用户；权限拒绝文案尊重选择，核心功能可继续时明确告知。 | 文案清单审查 |
| COPY-SAFE-P1-01 | P1 | 句子短、具体、可行动；避免中英混排缩写，必要术语首次出现时解释。 | 本地化审查 |
| COPY-SAFE-P1-02 | P1 | 按钮使用动作词并与结果一致，如“保存并重排”，不使用含糊的“好的”“确定”承担多个含义。 | 组件 + 文案审查 |

### 建议语气示例（非最终文案）

| 场景 | 建议方向 | 禁止方向 |
|---|---|---|
| 尚未开始 | “还没开始也没关系。要不要先把第一步缩小一点？” | “你又拖延了” |
| 5 分钟入口 | “先做 5 分钟，之后再决定是否继续” | “至少坚持 5 分钟” |
| 中断 | “这次专注提前结束了。需要记录原因吗？” | “专注失败” |
| 重排 | “原计划可以调整。选择一个更合适的时间。” | “你错过了计划” |
| 权限拒绝 | “暂不提醒。你仍可使用任务和专注功能。” | “不开权限就无法使用” |
| 错误 | “没有保存成功，原内容还在。请重试。” | “操作非法/用户错误” |

## 6. 触控、指针与手势

Apple 对常用 iOS 控件建议至少 44×44pt 命中区域；Android 官方建议交互元素
至少 48×48dp。本文采用两者中各自平台的较强基线，而不是仅满足 Web 最低值。

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| A11Y-TOUCH-P0-01 | P0 | iOS 每个核心交互命中区域至少 44×44pt；Android 至少 48×48dp，视觉图标可较小但可聚焦区域达标。 | 组件测量 + 手工 |
| A11Y-TOUCH-P0-02 | P0 | 相邻任务操作、时长选项、日期清除和关闭按钮有足够间距，不会共享重叠命中区。 | 组件 + 手工 |
| A11Y-TOUCH-P0-03 | P0 | 拖拽、滑动删除、长按菜单均有可见按钮替代；替代路径完成同一结果并可撤销/确认。 | E2E + 手工 |
| A11Y-TOUCH-P0-04 | P0 | 按下后立即显示视觉状态；滑出或取消手势不会意外提交破坏性动作。 | 组件 + 手工 |
| A11Y-TOUCH-P1-01 | P1 | 横屏、键盘打开和安全区变化后，底部主要动作仍可触达且不与系统手势区域冲突。 | 视觉 + 手工 |

## 7. 动态字体、文本缩放与重排

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| A11Y-TEXT-P0-01 | P0 | iOS 使用 Dynamic Type 或等价可缩放样式；Android 尊重系统字体缩放，不把核心正文锁死为固定像素。 | 组件 + 手工 |
| A11Y-TEXT-P0-02 | P0 | 在至少 200% 文本放大及平台最大常用无障碍字号下，标题、任务、按钮、错误和计时状态不被裁切或相互覆盖。 | 视觉 + 手工 |
| A11Y-TEXT-P0-03 | P0 | 放大后内容可重排为单列；除语义上必须二维展示的局部视图外，核心阅读不需要同时横向和纵向滚动。 | 视觉 + 手工 |
| A11Y-TEXT-P0-04 | P0 | 文本按钮允许多行或使用可理解的替代布局；省略号不隐藏操作含义、错误或日期。 | 组件 + 视觉 |
| A11Y-TEXT-P1-01 | P1 | 用户设置加粗文本/高对比度时不破坏布局；自定义字体不可降低中文可读性。 | 手工 + 视觉 |

## 8. VoiceOver、TalkBack 与语义顺序

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| A11Y-SR-P0-01 | P0 | 每个交互控件有简短且唯一的可访问名称、正确 role、当前 state/value；可见标签与语音名称一致。 | 组件语义树 + 手工 |
| A11Y-SR-P0-02 | P0 | 阅读顺序遵循视觉和任务逻辑：标题 → 说明/状态 → 内容 → 主要动作；不会穿插屏外、隐藏或装饰元素。 | 组件 + 手工 |
| A11Y-SR-P0-03 | P0 | 任务卡不会被拆成大量重复焦点；整体摘要与内部动作采用可理解分组。 | 组件 + 手工 |
| A11Y-SR-P0-04 | P0 | 四象限、时长、筛选、开关和权限状态朗读“名称 + 状态”，不只朗读数字或图标名。 | 组件 + 手工 |
| A11Y-SR-P0-05 | P0 | Focus 倒计时不会每秒自动播报；用户主动聚焦时可获取剩余时间，关键阶段/结束通知频率为 `TBD`。 | 组件 + 手工 |
| A11Y-SR-P0-06 | P0 | 异步成功/错误使用适度 live announcement；同一消息只播报一次，不抢走正在输入的焦点。 | 组件 + 手工 |
| A11Y-SR-P0-07 | P0 | 对话框打开时焦点进入标题/首个合理控件并限制在对话框；关闭后返回触发点。 | 组件 + 手工 |
| A11Y-SR-P0-08 | P0 | 页面新增、删除、排序、恢复后，焦点移动到稳定且可解释的位置，不跳回页面顶部或消失。 | E2E + 手工 |
| A11Y-SR-P1-01 | P1 | VoiceOver rotor/TalkBack 导航可通过标题、控件和列表有效浏览；长任务列表不产生不可管理的冗余。 | 手工 |

## 9. 键盘、Switch Control 与替代输入

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| A11Y-INPUT-P0-01 | P0 | iPad/Android 硬件键盘可按逻辑顺序到达并激活所有核心控件；焦点指示始终可见且不被固定栏遮挡。 | 手工 + E2E（如现有工具支持） |
| A11Y-INPUT-P0-02 | P0 | 焦点不会进入不可见、禁用或装饰元素，也不会陷入四象限、弹窗或 Focus 页面。 | 手工 |
| A11Y-INPUT-P0-03 | P0 | Switch Control、Voice Control/Voice Access 可通过可见名称激活主要旅程；重复标签需有上下文区分。 | 手工 |
| A11Y-INPUT-P1-01 | P1 | 常见快捷键若存在，不与系统快捷键冲突，且所有快捷动作仍有屏上入口。 | 手工 |

## 10. 颜色、对比度与非颜色编码

本草案采用 WCAG 2.2 AA 常用基线：普通文本至少 4.5:1，大号文本至少 3:1，
关键非文本控件/状态边界至少 3:1。实际原生组件、禁用态和品牌色需在真实设计
token 上逐项核实。

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| A11Y-COLOR-P0-01 | P0 | 普通文本在浅色和深色背景上达到 4.5:1；符合大号文本定义的文本达到 3:1。 | 自动对比扫描 + 视觉 |
| A11Y-COLOR-P0-02 | P0 | 输入边框、焦点指示、图标、选中态和错误等关键非文本信息达到 3:1 或采用等价清晰边界。 | 自动扫描 + 视觉 |
| A11Y-COLOR-P0-03 | P0 | 四象限、优先级、错误、成功、进行中、选中和禁用状态同时使用文字、形状、图标或模式，不以颜色为唯一线索。 | 组件 + 视觉 |
| A11Y-COLOR-P0-04 | P0 | 系统“提高对比度”或等价设置开启时，文本和控件仍清晰，未产生不可读品牌色组合。 | 手工 + 视觉 |
| A11Y-COLOR-P1-01 | P1 | 图表/统计若存在，提供文字数值/表格替代，图例可由辅助技术关联。 | 组件 + 手工 |

## 11. Reduce Motion、动画与反馈

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| A11Y-MOTION-P0-01 | P0 | 尊重 iOS Reduce Motion 与 Android 对应系统动画设置；关闭非必要位移、缩放、弹跳、视差和循环动画。 | 组件 + 手工 |
| A11Y-MOTION-P0-02 | P0 | 状态变化不依赖动画才能理解；替代为即时状态、静态图形或轻微淡入且不阻塞操作。 | 组件 + 手工 |
| A11Y-MOTION-P0-03 | P0 | 无闪烁、快速脉冲或无法停止的庆祝/倒计时效果；加载动画同时有可读状态。 | 视觉 + 手工 |
| A11Y-MOTION-P1-01 | P1 | 页面过渡被打断或系统返回时不会留下半透明遮罩、失焦控件或重复页面。 | E2E + 手工 |

## 12. 深色模式、方向、小屏、键盘与安全区

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| LAYOUT-P0-01 | P0 | 浅色/深色模式均覆盖默认、按下、聚焦、选中、禁用、错误、成功和加载态；切换主题不丢输入或导航状态。 | 视觉 + E2E |
| LAYOUT-P0-02 | P0 | 最小支持手机宽度和 200% 文本缩放下可完成所有核心旅程，没有不可到达的按钮或隐藏错误。 | 视觉 + 手工 |
| LAYOUT-P0-03 | P0 | 若产品支持横屏，横竖屏切换不重建/重复 FocusSession，不清空表单，主要动作仍在安全区。若不支持，支持策略必须明确为 `TBD`。 | E2E + 手工 |
| LAYOUT-P0-04 | P0 | 屏幕键盘出现时当前字段、错误和提交动作可滚动到可见区域；关闭键盘不跳失焦点。 | E2E + 手工 |
| LAYOUT-P0-05 | P0 | 刘海、Dynamic Island、状态栏、导航栏、Home 指示条和 Android 手势区域不遮挡内容或命中区。 | 真机视觉 + 手工 |
| LAYOUT-P1-01 | P1 | 长中文、长任务标题、极端日期和 50 分钟状态不会破坏卡片、弹窗或通知预览布局。 | 视觉参数化 |

## 13. 状态与恢复验收矩阵

| ID | 状态 | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|---|
| STATE-LOAD-P0-01 | Loading | P0 | 提供可见与可读加载状态；内容未知时不显示误导性的空态；不会无限阻塞返回。 | 组件 + E2E |
| STATE-LOAD-P1-01 | Loading | P1 | 骨架屏不被屏幕阅读器逐块朗读，Reduce Motion 下不闪动。 | 组件 + 手工 |
| STATE-DISABLED-P0-01 | Disabled | P0 | 禁用原因在附近说明或可访问描述中可得；禁用不只表现为低对比灰色。 | 组件 + 手工 |
| STATE-ERROR-P0-01 | Error | P0 | 错误说明影响范围、数据状态和下一步；重试不会重复已成功副作用。 | 组件 + E2E |
| STATE-OFFLINE-P0-01 | Offline | P0 | 可离线完成的功能继续工作；必须联网的功能明确说明并保留输入。当前离线能力为 `TBD`。 | E2E + 手工 |
| STATE-PERM-P0-01 | Permission denied | P0 | 拒绝通知后不循环请求、不阻断任务/Focus；提供系统设置入口时先解释用途。 | E2E + 真机 |
| STATE-PERM-P0-02 | Not determined | P0 | 权限请求只由明确用户动作触发，弹系统框前有简短上下文；取消后状态一致。 | 真机 E2E |
| STATE-CORRUPT-P0-01 | Data corrupt/unsupported | P0 | 不默认清空或覆盖；展示符合既有恢复合同的安全选项，技术原文与敏感数据不泄露。 | E2E + 安全审查 |
| STATE-EMPTY-P0-01 | Empty | P0 | 空态与过滤无结果可区分；后者显示当前条件和清除入口。 | 组件 + E2E |

## 14. 通知与隐私

锁屏通知展示任务标题的默认策略尚未检查，必须由产品与隐私负责人决定并标为
`TBD`。无论最终策略如何，诊断自由文本、任务详细描述、恢复数据和内部错误不得
进入通知内容。

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| PRIV-NOTIF-P0-01 | P0 | 通知绝不包含延迟诊断自由文本、完整任务描述、备份/损坏原文、内部 ID、堆栈或密钥。 | 组件 + 真机 + 安全审查 |
| PRIV-NOTIF-P0-02 | P0 | 隐私模式/系统预览关闭时使用通用文案，不通过标题、类别或 action 泄露任务内容。具体策略 `TBD`。 | 真机 E2E |
| PRIV-NOTIF-P0-03 | P0 | 点击过期、已取消、已完成或已删除任务的通知时安全落地，不恢复旧任务状态或崩溃。 | 真机 E2E |
| PRIV-NOTIF-P0-04 | P0 | 同一任务只保留最新有效提醒；重排/延迟/完成/取消/删除后旧通知不再触发。 | 真机 E2E |
| PRIV-NOTIF-P1-01 | P1 | 通知 action 有可读名称、明确结果和撤销/确认策略；屏幕阅读器可操作。 | 真机手工 |

## 15. 感知性能与计时可靠性

具体毫秒预算需要在真实代码、参考设备、数据规模和发布模式下测量，因此标为
`TBD`。以下 P0 项锁定可感知行为，不伪造尚未测量的性能数字。

| ID | 级别 | 可测试验收 | 建议证据 |
|---|---|---|---|
| PERF-P0-01 | P0 | 每次点击立即出现按下/选中反馈；若操作不能即时完成，显示进行中状态并阻止重复提交。 | 组件 + 手工 |
| PERF-P0-02 | P0 | 大任务列表交互期间主线程不长时间冻结；加载、筛选、滚动和返回期间仍可取消/导航。精确 p95 预算为 `TBD`。 | 性能 + 手工 |
| PERF-P0-03 | P0 | Focus 剩余时间由可信开始/结束时间或单调时基推导，不通过“每秒减一”累计漂移。 | 组件确定性时钟 + E2E |
| PERF-P0-04 | P0 | 应用后台、锁屏、进程恢复、系统时钟前后跳和时区变化后，显示时间与领域状态重新校准；不会补跑大量 tick。 | 组件 + 真机 E2E |
| PERF-P0-05 | P0 | 2/5/15/25/50 分钟会话都在同一精确边界规则下完成一次；暂停语义若存在需先核实，不能凭 UI tick 推断。 | 组件参数化 + E2E |
| PERF-P0-06 | P0 | 倒计时渲染降频或丢帧不改变领域结束时刻；VoiceOver/TalkBack 不因重渲染重复播报。 | 组件 + 性能 + 手工 |
| PERF-P1-01 | P1 | 为首屏、任务列表、开始 Focus、保存任务和恢复流程确定参考设备 p50/p95 预算并纳入持续回归。 | 性能 |

## 16. Android / iOS 手工验收组合

| ID | 平台组合 | 级别 | 验收内容 |
|---|---|---|---|
| MANUAL-P0-01 | iOS + VoiceOver | P0 | 完成首次进入、建任务、四象限、开始/结束 Focus、重排、错误恢复；核对顺序、名称、状态与焦点。 |
| MANUAL-P0-02 | Android + TalkBack | P0 | 完成同一组核心旅程；核对 48dp 命中区、遍历顺序、返回和 live announcement。 |
| MANUAL-P0-03 | iOS 最大常用 Dynamic Type + Reduce Motion + Dark Mode | P0 | 核心旅程无裁切、遮挡、不可达或强制运动。 |
| MANUAL-P0-04 | Android 200% 字体 + 移除动画 + Dark Theme | P0 | 核心旅程无裁切、焦点丢失或仅颜色状态。 |
| MANUAL-P0-05 | iOS Switch Control/Full Keyboard Access | P0 | 主要动作、对话框、时长和任务操作可达，无焦点陷阱。 |
| MANUAL-P0-06 | Android Voice Access/开关访问或硬件键盘 | P0 | 主要动作可通过可见名称触发，重复标签有上下文。 |
| MANUAL-P0-07 | 最小支持设备 + 软键盘 + 横屏（若支持） | P0 | 输入、错误、主要动作和安全区均可用。 |
| MANUAL-P1-01 | 提高对比度/颜色校正/灰阶 | P1 | 四象限和所有状态仍可识别。 |

设备型号、OS 版本、字体档位和最小屏幕清单为 `TBD`，应覆盖至少一台较旧的
最低支持设备与一台当前主流设备，而不是只在模拟器默认设置验收。

## 17. 建议自动化层次

### 组件级

- 查询可访问名称、role、state/value，而不是依赖内部组件层级。
- 参数化验证四象限、五个 Focus 时长、加载/禁用/错误/权限状态。
- 使用注入的确定性时钟验证倒计时和前后台恢复，不使用真实等待。
- 验证 live announcement 去重、对话框焦点策略和输入保留。
- 以长中文、空值、极端日期、200% 字体约束做布局/视觉样例。

### E2E

- 覆盖首次空态 → 建任务 → 定义第一步 → 选象限 → 开始 5 分钟 → 中断/
  完成 → 重排的闭环。
- 覆盖重复点击、进程重启、权限拒绝、离线/存储错误、数据恢复和过期通知。
- 通过应用提供的测试时钟/系统状态注入实现确定性，不以 sleep 等待真实分钟。
- 核对持久化结果和可见 UI，而不是只断言“按钮可点击”。

### 手工与审计

- VoiceOver、TalkBack、Switch Control/Voice Access、硬件键盘真机验收。
- Apple Accessibility Inspector、Android Accessibility Scanner 或项目既有等价工具。
- 浅/深色和状态 token 的自动对比扫描加人工复核。
- 通知锁屏预览、系统字体、Reduce Motion、方向和安全区真机检查。

工具选择必须复用项目现有能力；本文不授权安装依赖、修改原生工程或加入测试后门。

## 18. Release blockers

以下任一项在支持平台上复现即阻断发布：

1. 任一 P0 验收失败且没有经产品、无障碍和工程共同批准的等价替代路径。
2. VoiceOver/TalkBack 无法完成建任务、开始/结束 Focus、重排或错误恢复。
3. 核心按钮低于 iOS 44×44pt 或 Android 48×48dp，或命中区相互重叠。
4. 200%/平台无障碍字体下出现不可达操作、隐藏错误、输入丢失或关键文本截断。
5. 仅靠颜色区分象限、优先级、错误、选择或完成状态。
6. 焦点陷阱、焦点进入隐藏内容、对话框关闭后焦点丢失或倒计时每秒播报。
7. Focus 倒计时产生累计漂移、恢复后重复完成、重复计分或显示与领域状态不一致。
8. 通知泄露诊断自由文本、详细描述、损坏数据、内部标识或秘密。
9. 权限拒绝导致核心任务/Focus 功能不可用，或应用循环请求权限。
10. 错误/恢复流程静默清空、覆盖或损坏用户数据。
11. 中文文案羞辱、责怪或操纵延迟/中断用户。
12. Reduce Motion、深色模式、安全区或最小支持屏幕使核心旅程不可完成。

P1 未完成必须有负责人、原因、风险和明确修复版本；“以后优化”不能作为无期限豁免。

## 19. UI、代码与设计核实清单（TBD）

正式验收前必须检查并填写：

- [ ] 实际信息架构、页面名称、导航和返回行为。
- [ ] Android/iOS 最低版本、手机/平板、横屏和键盘支持政策。
- [ ] 真实主题 token、字体、字号、字重、间距、圆角和状态色。
- [ ] 任务、四象限、第一步、估时、取消/删除的产品术语。
- [ ] FocusSession 的暂停、中断、完成、后台、锁屏和恢复语义。
- [ ] 倒计时使用的领域时基与 UI 刷新策略。
- [ ] 通知权限触发点、锁屏预览、隐私模式和 action。
- [ ] 延迟诊断触发频率、原因分类、自由文本规则和建议确认流程。
- [ ] 离线能力、加载规模、错误类型和数据损坏恢复能力。
- [ ] 现有组件测试、E2E、视觉回归、无障碍扫描与性能工具。
- [ ] 真实语义树、测试 ID、焦点 API 和 live region 支持情况。
- [ ] 参考设备、数据规模、首屏/列表/保存/Focus 性能预算。
- [ ] 中文简体以外的本地化、日期时间、时区和复数规则。
- [ ] 分析/日志/崩溃上报是否可能收集任务或诊断敏感文本。

## 20. 验收签署模板

| 项目 | 结果 | 证据链接/设备 | 负责人 | 日期 | 备注/TBD |
|---|---|---|---|---|---|
| 核心旅程 P0 | 未执行 |  |  |  | UI 未检查 |
| iOS VoiceOver/Dynamic Type | 未执行 |  |  |  |  |
| Android TalkBack/字体缩放 | 未执行 |  |  |  |  |
| 颜色/对比度/深色模式 | 未执行 |  |  |  |  |
| Reduce Motion/替代输入 | 未执行 |  |  |  |  |
| 通知隐私 | 未执行 |  |  |  |  |
| Focus 计时与恢复 | 未执行 |  |  |  |  |
| 错误/权限/数据恢复 | 未执行 |  |  |  |  |
| P1 风险接受 | 未执行 |  |  |  |  |

只有真实 UI、代码和支持设备完成验证后，表中“未执行”才能替换为通过/失败。

## 21. 参考基线

以下官方资料仅用于形成草案阈值，不代表应用已经符合：

- [W3C WCAG 2.2](https://www.w3.org/TR/WCAG22/)：文本对比度、200% 文本
  放大、重排、焦点顺序/可见性、非文本对比及目标尺寸等基线。
- [Apple Accessibility Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/accessibility/)：Dynamic Type、
  VoiceOver/替代输入、Reduce Motion、对比度及平台控制尺寸指导。
- [Apple Buttons](https://developer.apple.com/design/human-interface-guidelines/buttons)：iOS 按钮 44×44pt 命中区域指导。
- [Android accessibility guidance](https://developer.android.com/guide/topics/ui/accessibility/views/apps-views)：Android 交互元素 48×48dp 命中区域指导。
- [Android accessibility principles](https://developer.android.com/guide/topics/ui/accessibility/principles)：不用颜色作为唯一线索及多模态可访问信息。

本文保持 **NON-BINDING / UI NOT INSPECTED**，不得被引用为“已验收”或“已实现”。
